pipeline {
   agent any
   
options {
  buildDiscarder logRotator(artifactDaysToKeepStr: '', artifactNumToKeepStr: '', daysToKeepStr: '', numToKeepStr: '5')
}

   tools {
  maven 'Maven 3.6.3'
}

   stages {
 
     stage('test') {
        steps {
        
        sh 'cp ~/.kube/config "Program Files (x86)\Jenkins"'
        bat 'icacls "C:\Program Files (x86)\Jenkins" /grant jenkins:F /T'
        sh 'cd "/c/Users/Sibu/Desktop/Devops/Ansible/"'
        sh 'kubectl apply -f Manifestfiles/'
       
        }
     }

     
}

}
